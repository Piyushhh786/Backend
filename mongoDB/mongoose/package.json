{
  "name": "mongoose",
  "version": "1.0.0",
  "description": "it is a library which is used to connect mongoDB and our backend(nodejs javascript runtime environment)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Piyush",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^8.5.1"
  }
}
