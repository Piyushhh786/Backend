<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h1>Quora </h1>
    <% for(post of posts){ %>
        <div class="post">
            <h3 class="name">
                @<%= post.username %>
            </h3>
            <h2 class="content">
                content:<%= post.content %>
            </h2>

            <a href="http://localhost:8080/post/<%=post.id%>">show in details</a>
            <a href="http://localhost:8080/post/<%=post.id%>/edit">edit content</a>
            <form method="post" action="http://localhost:8080/post/<%=post.id %>/delete?_method=DELETE">
                <button>Delete</button>
            </form>

        </div>
        <% } %>
            <a href="http://localhost:8080/post/new">Create new Post</a>
</body>

</html>